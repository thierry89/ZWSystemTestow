<%= form_for(@podejscy) do |f| %>
  <% if @podejscy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@podejscy.errors.count, "error") %> prohibited this podejscy from being saved:</h2>

      <ul>
      <% @podejscy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
