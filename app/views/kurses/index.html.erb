<h1>Listing kurses</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @kurses.each do |kurs| %>
  <tr>
    <td><%= link_to 'Show', kurs %></td>
    <td><%= link_to 'Edit', edit_kurs_path(kurs) %></td>
    <td><%= link_to 'Destroy', kurs, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Kurs', new_kurs_path %>
