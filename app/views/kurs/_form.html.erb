<%= form_for(@kur) do |f| %>
  <% if @kur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kur.errors.count, "error") %> prohibited this kur from being saved:</h2>

      <ul>
      <% @kur.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
